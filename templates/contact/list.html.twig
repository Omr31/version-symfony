{% extends 'back.html.twig' %}
{% block body %}
<table>
    <thead>
        <tr>
            <th>Nom</th>
            <th>Email</th>
            <th>Message</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for contact in contacts %}
            <tr>
                <td>{{ contact.nom }}</td>
                <td>{{ contact.email }}</td>
                <td>{{ contact.message }}</td>
                <td>
                                   <a href="{{ path('contact_edit', {id: contact.id}) }}" class="btn btn-primary">Modifier</a>
                                   <a href="{{ path('contact_delete', {id: contact.id}) }}" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this contact?')">Supprimer</a>
                                   <button><a href="mailto: {{ contact.email }}">Répondre</a></button>
                              </td>

                
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}

